<% if @goal.errors.any? -%>
  
  /*Hide the flash notice div*/
  $("#flash_notice").hide(300);

  /*Update the html of the div post_errors with the new one*/
  $("#goal-errors").append("<%= escape_javascript(error_messages_for (@goal))%>");

  /*Show the div flash_error*/
  $("#goal_errors").show(300);
<% else -%>
  
  /*Hide the div post_errors*/
  $("#goal-errors").hide(300);

  /*Update the html of the div flash_notice with the new one*/
  $("#flash_notice").append("<%= escape_javascript(flash[:notice])%>");

  /*Show the flash_notice div*/
  $("#flash_notice").show(300);

  /*Clear the entire form*/
  $("#goal_text").val("");

  /*Replace the html of the div goals with the updated new one*/
  $("#goals").html("<%= escape_javascript( render "goals/goal" ) %>");
  $("#goal-modal").modal('toggle')

  
  

<% end -%>
