<%- model_class = Goal.new.class -%>
<% content_for :sidebar do %>
	Menu Needed
<% end %>

<%= content_for :top_left do %>

  <h3><%=t '.title', :default => model_class.model_name.human.pluralize %></h3>

<% end %>
<% content_for :top do%>
<div class="pull-right">
<a class="btn btn-primary" data-toggle="modal" href="#goal-modal">New Goal</a>
</div>
<% end %>
<div id="goal-modal" class="modal hide fade">
	
	<%= render "goals/form" %>
</div>


<table class="table table-striped">

    <tr>
    
      <th><%= model_class.human_attribute_name(:category_id) %></th>
      <th><%= model_class.human_attribute_name(:value) %></th>
      

      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  
   <tbody id="goals">
  
    <%= render "goals/goal" %>
      
   </div>
  </tbody>
</table>


