  <% for mpost in @mposts %>
    <div class="post-block">
  
      <div class="post-body">
      <%= mpost.user %>
      <%= mpost.content %>
      </div>
      </br>
      <div class="controls">
      <%= link_to "Edit", edit_mpost_path(mpost), :remote => true %><%= link_to "Destroy", mpost, :remote => true, :confirm => 'Are you sure?', :method => :delete %>
     </div>
      
    </div>
  <% end %>
