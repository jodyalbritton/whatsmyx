                <ul class="nav pull-left">
	                 <% if user_signed_in? %>
	                <li><%= link_to "Feed", root_path %></li>
	                <li><%= link_to "Stats", stats_path %></li>
	                <li><%= link_to "Community", community_index_path %></li>
	                <%end%>
                </ul>
                <ul class="nav pull-right">
	                <li class="dropdown">
                     <a href="#"
                          class="dropdown-toggle"
                          data-toggle="dropdown">
                          Account
                          <b class="caret"></b>
                    </a>
                   <ul class="dropdown-menu">
                  <% if user_signed_in? %>
                    <li>
                    <%= link_to('Edit account', edit_user_registration_path) %>
                    </li>
                    <li>
                    <%= link_to('Edit Profile', edit_user_registration_path(:anchor => "profile"))%>
                    </li>
                     <li>
                    <%= link_to('Change Password', edit_user_registration_path(:anchor => "wordpass"))%>
                    </li>
                     <li>
                    <%= link_to('Auth Providers', services_path )%>
                    </li>
                    <li>
                    <%= link_to('Logout', destroy_user_session_path, :method=>'delete') %>  
                    </li>
                  <% else %>
                    <li>
                    <%= link_to('Login', new_user_session_path)  %>  
                    </li>
                  <% end %>
                  <% if user_signed_in? %>
                     <% if current_user.has_role? :admin %>
                      <li>
                      <%= link_to('Admin', users_path) %>
                      </li>
                    <% end %>
                  <% else %>
                    <li>
                    <%= link_to('Sign up', new_user_registration_path)  %>
                    </li>
                  <% end %>
                  </ul>
                  </li>
                </ul>
