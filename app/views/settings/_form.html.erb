
<%= simple_form_for @current_user.settings do |f| %>

  <%= f.input :acct_enabled %>
 

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
 
  </div>
<% end %>